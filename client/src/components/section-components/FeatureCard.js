import {
  Box,
  chakra,
  Flex,
  SimpleGrid,
  Stat,
  StatLabel,
  StatNumber,
  useColorModeValue,
  Link,
  Heading,
  Text,
} from "@chakra-ui/react";

import React from "react";
import { Table } from "react-bootstrap";

const FeatureCard = (props) => {
  console.log("Features.New", props);
  const { title, value, shades } = props;

  return <></>;
};

export default FeatureCard;
